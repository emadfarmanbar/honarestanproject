<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>خطای سیستمی - هنرستان آسودگی</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Google Fonts - Vazir -->
  <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@100;300;400;500;700;900&display=swap" rel="stylesheet">
  <!-- Tailwind Config -->
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Vazirmatn', 'sans-serif'],
          },
          colors: {
            primary: {
              light: '#4f46e5',
              DEFAULT: '#4338ca',
              dark: '#3730a3',
            },
            secondary: {
              light: '#f59e0b',
              DEFAULT: '#d97706',
              dark: '#b45309',
            },
            danger: {
              light: '#ef4444',
              DEFAULT: '#dc2626',
              dark: '#b91c1c',
            }
          },
          boxShadow: {
            'neumorphism': '8px 8px 16px #d1d9e6, -8px -8px 16px #ffffff',
            'neumorphism-inset': 'inset 4px 4px 8px #d1d9e6, inset -4px -4px 8px #ffffff',
          }
        }
      }
    }
  </script>
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #e4e8f0 100%);
    }
    .glass-card {
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border-radius: 1rem;
      border: 1px solid rgba(255, 255, 255, 0.18);
    }
    .btn-glow:hover {
      box-shadow: 0 0 15px rgba(220, 38, 38, 0.6);
    }
    .feature-card {
      @apply bg-white/80 rounded-xl p-4 flex flex-col items-center justify-center shadow-neumorphism hover:shadow-neumorphism-inset transition cursor-pointer;
    }
    .btn-primary {
      @apply bg-gradient-to-r from-danger to-danger-dark text-white px-8 py-3 rounded-full font-bold flex items-center justify-center transition hover:bg-gradient-to-l hover:shadow-lg btn-glow;
    }
    .error-animation {
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.05); }
      100% { transform: scale(1); }
    }
  </style>
</head>
<body class="font-sans min-h-screen flex flex-col items-center justify-center p-4">

  <!-- Main Card -->
  <div class="glass-card w-full max-w-2xl p-8 shadow-2xl rounded-2xl overflow-hidden relative error-animation">
    <!-- Decorative Elements -->
    <div class="absolute -top-20 -right-20 w-40 h-40 bg-danger-light rounded-full opacity-10"></div>
    <div class="absolute -bottom-20 -left-20 w-60 h-60 bg-secondary-light rounded-full opacity-10"></div>
    
    <!-- Logo Section -->
    <div class="flex flex-col items-center mb-8 relative z-10">
      <div class="w-28 h-28 bg-white rounded-full shadow-neumorphism flex items-center justify-center mb-4">
        <i class="fas fa-exclamation-triangle text-4xl text-danger"></i>
      </div>
      <div class="text-center">
        <h1 class="text-3xl md:text-4xl font-black text-transparent bg-clip-text bg-gradient-to-r from-danger to-danger-dark mb-2">
          خطای سیستمی
        </h1>
        <p class="text-gray-600 font-medium">هنرستان فنی و حرفه‌ای آسودگی</p>
      </div>
    </div>
    
    <!-- Error Content -->
    <div class="relative z-10 text-center">
      <div class="text-6xl font-bold text-danger mb-6">
        <i class="fas fa-bug"></i>
        خطای 500
      </div>
      <h2 class="text-2xl font-bold text-gray-800 mb-4">
        مشکلی در سرور پیش آمده است!
      </h2>
      
      <div class="bg-white/80 rounded-xl p-6 mb-6 shadow-neumorphism text-right">
        <p class="text-gray-600 mb-3">
          <i class="fas fa-info-circle text-danger mr-2"></i>
          متأسفانه سرور با مشکل مواجه شده و نمی‌تواند درخواست شما را پردازش کند.
        </p>
        <p class="text-gray-600">
          <i class="fas fa-clock text-danger mr-2"></i>
          تیم فنی ما در حال بررسی مشکل است. لطفاً چند دقیقه دیگر مجدداً تلاش کنید.
        </p>
      </div>
      
      <!-- Technical Details (Optional) -->
      <details class="mb-6 bg-white/80 rounded-xl p-4 shadow-neumorphism text-right cursor-pointer">
        <summary class="font-medium text-danger">مشاهده جزئیات فنی (اختیاری)</summary>
        <div class="mt-3 p-3 bg-gray-50 rounded-lg text-sm text-gray-600 text-left font-mono">
          <p>Error: Internal Server Error</p>
          <p>Time: <span id="error-time"></span></p>
          <p>Request ID: HS-<span id="request-id"></span></p>
        </div>
      </details>
      
      <!-- Quick Actions -->
      <div class="grid grid-cols-2 gap-4 mb-8">
        <a href="/" class="feature-card">
          <i class="fas fa-home text-danger text-2xl mb-2"></i>
          <span>صفحه اصلی</span>
        </a>
        <a href="mailto:support@asoodegi.ir" class="feature-card">
          <i class="fas fa-headset text-danger text-2xl mb-2"></i>
          <span>پشتیبانی فنی</span>
        </a>
      </div>
      
      <!-- CTA Button -->
      <div class="flex flex-col items-center">
        <button onclick="window.location.reload()" class="btn-primary mb-4">
          تلاش مجدد
          <i class="fas fa-sync-alt mr-2"></i>
        </button>
        <p class="text-sm text-gray-500">
          <i class="fas fa-clock mr-1"></i>
          زمان فعلی: <span id="current-time"></span>
        </p>
      </div>
    </div>
  </div>
  
  <!-- Footer -->
  <div class="mt-8 text-center text-gray-500 text-sm">
    <p>سیستم مدیریت هنرستان آسودگی © ۱۴۰۳</p>
    <div class="flex justify-center gap-4 mt-2">
      <a href="#" class="hover:text-danger transition"><i class="fab fa-instagram"></i></a>
      <a href="#" class="hover:text-danger transition"><i class="fab fa-telegram"></i></a>
      <a href="mailto:info@asoodegi.ir" class="hover:text-danger transition"><i class="fas fa-envelope"></i></a>
    </div>
  </div>

  <script>
    // نمایش زمان فعلی
    function updateTime() {
      const now = new Date();
      const timeString = now.toLocaleTimeString('fa-IR');
      document.getElementById('current-time').textContent = timeString;
    }
    
    // تولید یک شناسه تصادفی برای درخواست
    function generateRequestId() {
      return Math.floor(100000 + Math.random() * 900000);
    }
    
    // تنظیم جزئیات خطا
    document.getElementById('error-time').textContent = new Date().toLocaleString('fa-IR');
    document.getElementById('request-id').textContent = generateRequestId();
    
    // به روز رسانی زمان هر ثانیه
    updateTime();
    setInterval(updateTime, 1000);
  </script>

</body>
</html>